{
  "name": "medical-card-backend",
  "version": "1.0.0",
  "main": "dist/index.js",
  "scripts": {
    "dev": "nodemon --watch 'src/**/*.ts' --exec ts-node src/index.ts",
    "start": "node dist/index.js",
    "prisma": "prisma generate",
    "db:migrate": "prisma migrate dev",
    "build": "tsc"
  },
  "dependencies": {
    "@prisma/client": "^5.0.0",
    "apollo-server": "^3.12.0",
    "graphql": "^16.6.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1",
    "prisma": "^5.0.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.3.2"
  }
}
